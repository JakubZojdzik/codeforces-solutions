#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define debug(a) cout << #a << " = " << a << '\n'

ll a[100007];
vector<int> wsp[100007];

int main()
{
    cout.tie(0);
    cin.tie(0)->sync_with_stdio(0);
    
    ll t, n;
    cin >> t;
    wsp[0].push_back(1);
    wsp[1].push_back(1);
    wsp[1].push_back(1);
    for(int i = 2; i < 100007; i++)
    {
        for(int i = 0; i < n; i++)
    }
    for(int i = 0; i < t; i++)
    {
        cin >> n;
        for(int i = 0; i < n; i++)
        {
            cin >> a[i];
        }


    }
}

/*

*/